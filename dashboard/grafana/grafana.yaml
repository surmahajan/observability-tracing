---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: default
data:
  elasticsearch.yaml: |-
    {
        "apiVersion": 1,
        "datasources": [
            {
                "name": "elastic",
                "type": "elasticsearch",
                "access": "proxy",
                "editable": true,
                "basicAuth" : true,
                "basicAuthUser" : "elastic",
                "orgId": 1,
                "url": "https://10.102.52.245:9200",
                "version": 1,
                "jsonData" : {
                  "tlsSkipVerify" : true,
                  "esVersion" : "7.0.0",
                  "timeField" : "startTimeMillis"
                },
                "secureJsonData" : {
                  "basicAuthPassword" : "7L3P43byI9Gyy3EBSz3J367f"
                }
            },
            {
                "name": "Jaeger",
                "type": "jaeger",
                "uid": "jaeger-spectra",
                "url": "http://jaeger-prod-query.default:16686",
                "editable": true,
                "isDefault": false
            }
        ]
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      name: grafana
      labels:
        app: grafana
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:latest
          ports:
            - name: http-grafana
              containerPort: 3000
              protocol: TCP
          resources:
            limits:
              memory: "1Gi"
              cpu: "1000m"
            requests:
              memory: 500M
              cpu: "500m"
          volumeMounts:
            - mountPath: /var/lib/grafana
              name: grafana-pv
            - mountPath: /etc/grafana/provisioning/datasources
              name: grafana-datasources
              readOnly: false
      volumes:
        - name: grafana-pv
          persistentVolumeClaim:
            claimName: grafana-pvc
        - name: grafana-datasources
          configMap:
            defaultMode: 420
            name: grafana-datasources
---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: default
spec:
  selector:
    app: grafana
  type: LoadBalancer
  ports:
    - port: 3000
      targetPort: http-grafana
      protocol: TCP
---